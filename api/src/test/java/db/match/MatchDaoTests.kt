package db.match

import org.junit.Test

class MatchDaoTests {

    // MOVE database shit into its own micro service?
    @Test
    fun `Test that we can fetch the most recent match from the match dao`() {
        // save some mock data into the match dao
    }
}